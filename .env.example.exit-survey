# Exit Survey Email Configuration
# Add these variables to your .env.local file

# Email addresses to receive exit survey feedback (comma-separated)
EXIT_SURVEY_EMAILS=your-email@example.com,team@example.com

# Email service configuration - Choose ONE option below:

# Option 1: Resend (Recommended)
RESEND_API_KEY=your_resend_api_key
EMAIL_FROM=noreply@yourstore.com

# Option 2: SMTP (Gmail, SendGrid, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@yourstore.com

# Fallback: If no email service is configured, feedback will be logged to console (development only)
